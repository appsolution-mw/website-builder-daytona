[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800

[agents.git-commit-manager]
description = "Stages and creates focused Conventional Commits for completed website-builder-daytona tasks."
config_file = "agents/git-commit-manager.toml"
nickname_candidates = ["Committer"]
