# T-20260505-010 - Durable project agent queue implementation

Status: In Progress
Date: 2026-05-05
Owner: AI Agent

Related files:
- docs/superpowers/specs/2026-05-05-durable-project-agent-queue-design.md
- docs/superpowers/plans/2026-05-05-durable-project-agent-queue.md
- prisma/schema.prisma
- lib/agent-runs/
- worker-agent/
- container/sandbox/broker/
- app/api/projects/
- app/project/[id]/page.tsx

## Scope

Implement the durable project-level agent queue plan on the
`feature/durable-project-agent-queue` worktree.

## Plan

- Execute the implementation plan task-by-task with subagent review gates.
- Keep commits small and scoped.
- Run focused verification after each task and final verification before
  completion.

## Notes

- Worktree: `.worktrees/durable-project-agent-queue`
- Baseline verified with `pnpm -F @wbd/broker test`: 15 test files and 101
  tests passed.
- Task 1 added durable queue Prisma enums, workspace/queue/run/attempt/event
  models, matching Postgres migration, and a schema smoke test.
- `pnpm prisma migrate dev --name add_durable_agent_queue` was not available
  because this environment has no `DATABASE_URL`; migration SQL was generated
  from Prisma diff output with a placeholder validation URL.
- Task 8 slice in progress: implement `lib/agent-runs/drain.ts` plus durable
  retry/skip state helpers in `lib/agent-runs/queue.ts`.
- Review fixes in progress: prevent duplicate execution on stale drain claims
  and fail/block runs when the execution adapter throws.

## Outcome

Pending.
