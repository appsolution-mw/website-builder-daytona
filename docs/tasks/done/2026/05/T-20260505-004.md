# T-20260505-004 - OpenHands Agent Configuration UI

Status: Done
Date: 2026-05-05
Owner: AI Agent

Related files:
- docs/tasks/active/T-20260505-004.md
- docs/superpowers/plans/2026-05-05-openhands-agent-configuration-ui.md
- prisma/schema.prisma
- lib/agent-config
- app/api/agent-config
- app/api/projects/[id]/agent-config
- container/sandbox
- app/agent-config
- app/project/[id]/page.tsx
- docs/changelog/2026-05.md

## Scope

Implement global and project-level OpenHands `AGENTS.md`, `.agents/skills`, and `.agents/agents` management according to the approved implementation plan.

## Plan

- Implement data model, resolver, materializer, and API routes.
- Implement runtime materialization and OpenHands bridge path updates.
- Implement global and project UI controls.
- Verify with focused tests, lint, and build where possible.

## Notes

Use sub-agent delegation with disjoint ownership areas. Do not commit unless explicitly requested.

## Outcome

Implemented global and project-level OpenHands configuration management.

- Added Prisma-backed storage for global `AGENTS.md`, skills, file agents, and per-project mode/enablement overrides.
- Added global `/agent-config` UI plus project-side agent config panel with effective preview and live sandbox file sync.
- Materialized effective config into `AGENTS.md`, `.agents/skills/<name>/SKILL.md`, and `.agents/agents/<name>.md` for sandbox create, restart, and fake-runtime respawn.
- Updated OpenHands bridge and sandbox entrypoint to prefer modern `.agents/*` paths while retaining legacy fallback behavior.
- Verified with Prisma validation/generate, focused Vitest suites, Runtime tests, Python bridge tests/compile, lint, diff check, and `pnpm build`.
