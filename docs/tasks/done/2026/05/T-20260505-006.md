# T-20260505-006 - Template sandbox provisioning fix

Status: Done
Date: 2026-05-05
Owner: AI Agent

Related files:
- app/api/projects/route.ts
- app/api/projects/__tests__/route.test.ts

## Scope

Investigate and fix the `sandbox provisioning failed` error when creating a new project from a template.

## Plan

- Trace the template project creation flow and sandbox provisioning path.
- Reproduce or isolate the failing branch with focused tests or commands.
- Implement the smallest root-cause fix.
- Verify with the narrowest relevant test/build command.

## Notes

- No sub-agent delegation: this is a focused debugging task and the available agent tool policy only allows spawning agents when explicitly requested.
- Local reproduction of the current template route returned 201; the historical failed row only stored the generic sanitized error.
- The route now maps known worker-agent failures to safe non-secret messages and clears the spawn timeout timer after success or failure.

## Outcome

Template project creation was verified locally through the real route. Known worker-agent provisioning failures now store a safe actionable message instead of only the generic fallback, and successful spawns no longer leave the timeout timer alive.
