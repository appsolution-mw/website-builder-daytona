# T-20260505-005 - Fix Agent Config HTTP 500

Status: Done
Date: 2026-05-05
Owner: AI Agent

Related files:
- docs/tasks/active/T-20260505-005.md
- app/api/projects/[id]/agent-config/route.ts
- app/api/agent-config/route.ts
- lib/agent-config
- prisma/migrations/20260505120000_add_agent_configuration/migration.sql
- docs/changelog/2026-05.md

## Scope

Investigate and fix the HTTP 500 shown when opening the project Agent config panel.

## Plan

- Check migration status and the route behavior against the local database.
- Identify whether the 500 is caused by schema drift, route validation, or runtime state.
- Apply the smallest safe fix and verify with focused checks.

## Notes

No sub-agent delegation for this bugfix; the failure path is narrow and tightly coupled.

## Outcome

Fixed for the local development database by applying
`20260505120000_add_agent_configuration` and marking it as applied. The
Agent config tables and enums now exist, and the project Agent config snapshot
path resolves successfully.

Verification:

- Confirmed all 6 Agent config tables and all 3 Agent config enums exist.
- Loaded a project Agent config snapshot through `lib/agent-config/db.ts`.
- Ran focused Agent config API/resolver/materializer tests: 4 files, 13 tests passed.

Note: `prisma migrate status` still reports pre-existing local migration drift
for older migrations unrelated to this Agent config failure.
