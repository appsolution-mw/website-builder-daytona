# T-20260505-001 - OpenHands chat replies restore

Status: Done
Date: 2026-05-05
Owner: AI Agent

Related files:
- container/sandbox/broker/python/openhands_bridge.py
- container/sandbox/broker/python/tests/test_openhands_bridge.py

## Scope

Diagnose and fix OpenHands turns that complete without showing an assistant reply in the chat UI.

## Plan

- Add a focused regression test for nested OpenHands message event text.
- Update the OpenHands bridge text extraction to surface assistant replies.
- Run focused verification for the bridge.

## Notes

The UI shows a completed OpenHands turn with zero output because no `agent.chunk` is emitted before `agent.done`.

## Outcome

Fixed the OpenHands bridge text extraction so nested assistant `MessageEvent`
content is emitted as chat chunks. Added a focused Python regression test,
verified the OpenHands broker tests, and rebuilt the local `wbd/sandbox:dev`
image.
