# T-20260507-002 - Diagnose stalled chat run

Status: Done
Date: 2026-05-07
Owner: AI Agent

Related files:
- .env
- lib/agent-runs
- lib/runtime/worker-pool

## Scope

Diagnose why a newly submitted chat message does not visibly progress after the worker URL configuration was adjusted.

## Plan

- Inspect current environment routing without exposing secrets.
- Check the latest project runs, queue state, and events.
- Verify worker-agent reachability for the configured route.
- Propose the smallest next fix.

## Notes

- `.env` no longer contains `WORKER_AGENT_URL`, so the local override was removed in the file.
- Local `127.0.0.1:4500` is still unreachable, while the assigned worker `100.127.25.102:4500` responds to health checks.
- The latest submitted run `cmoumknm10002k2lz03wdgxxf` exists and is `QUEUED`.
- The project queue is `BLOCKED` by the earlier failed run `cmouma3yr000846lzz57tsz9m`, so the new queued run cannot start yet.
- The assigned sandbox reports `status: "running"` directly from the worker-agent.

## Outcome

Root cause identified: the new chat message is queued behind a blocked failed run. Recommended next step is to restart `pnpm dev` after the `.env` change, then skip or retry the blocked run so the queue can drain and start the new run.
