# T-20260507-003 - Add queue unblock controls

Status: Done
Date: 2026-05-07
Owner: AI Agent

Related files:
- app/project/[id]/page.tsx
- app/api/projects/[id]/runs/[runId]/retry/route.ts
- app/api/projects/[id]/runs/[runId]/skip/route.ts

## Scope

Add minimal UI controls for retrying or skipping a blocked durable chat run from the project workspace.

## Plan

- Add focused coverage for blocked queue action state.
- Track project queue state in the workspace page.
- Render retry/skip controls when the queue is blocked.
- Wire controls to the existing API endpoints and refresh queue/events.

## Notes

- Added a small queue UI helper with focused coverage.
- The workspace now stores the latest project queue state from `/api/projects/:id/runs`.
- When the queue is blocked, the chat panel shows Retry and Skip controls wired to the existing run endpoints.
- The top status badge now shows `blocked` for blocked queues.

## Outcome

Completed. Blocked durable chat runs can now be retried or skipped from the project workspace UI.
