# T-20260507-005 - Diagnose Hetzner worker provisioning

Status: Done
Date: 2026-05-07
Owner: AI Agent

Related files:
- docs/tasks/active/T-20260507-005.md
- .env
- docs/changelog/2026-05.md

## Scope

Diagnose why the new Hetzner worker `worker-2` takes unusually long to provision.

## Plan

- Check server reachability and SSH access.
- Inspect cloud-init, Docker, and worker-agent container state.
- Identify the provisioning blocker from logs and runtime state.
- Recommend or apply the smallest safe fix.

## Notes

- Server `49.12.72.181` was reachable over SSH, but port `4500` initially refused connections.
- `cloud-init status --long` reported `scripts_user/runcmd` failure.
- Cloud-init output showed `ghcr.io/appsolution-mw/worker-agent:broker-host-fix` was not found, so no worker-agent container was created.
- `ghcr.io/appsolution-mw/worker-agent:latest` was pullable after the GitHub workflow completed.
- Re-ran the worker script with the valid `latest` tag; the worker-agent started and `/health` returned OK.
- The host database now marks `worker-2` as `READY` with a fresh heartbeat.

## Outcome

Completed. `worker-2` was recovered by replacing the invalid worker-agent image tag with `latest`, starting the worker-agent container, and updating local `.env` so future workers use a pullable image.
