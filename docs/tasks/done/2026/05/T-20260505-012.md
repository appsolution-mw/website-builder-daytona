# T-20260505-012 - Selectively integrate useful worktree changes

Status: Done
Date: 2026-05-05
Owner: AI Agent

Related files:
- app/api/library/
- app/library/
- components/library/
- lib/library/
- lib/agents/
- prisma/schema.prisma
- app/page.tsx
- next.config.ts
- ws-proxy/src/index.ts

## Scope

Selectively bring missing, still-useful changes from the remaining worktrees into
the current `main` without merging outdated branch state wholesale.

## Plan

- Integrate the missing OpenHands library and preset snapshot pieces where they
  fit the current durable queue architecture.
- Bring over small LAN/preview URL and WebSocket proxy host improvements if they
  remain useful and low-risk.
- Avoid overwriting newer `main` auth, GitHub, queue, task, and changelog work.
- Run focused tests, lint, and build.

## Notes

- `feat/global-openhands-library` is not in `main` and contains the larger useful
  missing feature area.
- `feat/github-auth-repo-integration` overlaps heavily with `main`; only small
  missing support utilities should be considered.

## Outcome

Completed selective integration without merging the old branches wholesale:
added authenticated Library API/UI, user-scoped library import/export,
workflow preset revisions and session snapshots, durable OpenHands run snapshot
binding, LAN-friendly preview/proxy configuration, and focused regression tests.
Skipped outdated GitHub/Auth branch overlap and the hard-coded OpenHands
`allowedDevOrigins` IP.
