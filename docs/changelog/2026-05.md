# Changelog 2026-05

> Imported from legacy `CHANGELOG.md`. The full original file is preserved at `docs/archive/CHANGELOG.legacy.md`.

## 2026-05-07

### T-20260507-001 - OpenHands chat send failure fixed

- Converted OpenHands persisted conversation IDs from CLI strings to UUIDs and
  rejected non-UUID provider session IDs before enqueueing runs. Rebuilt the
  local `wbd/sandbox:dev` image.
- Affected files: `container/sandbox/broker/python/openhands_bridge.py`,
  `container/sandbox/broker/python/tests/test_openhands_bridge.py`,
  `app/api/projects/[id]/runs/route.ts`,
  `app/api/projects/[id]/runs/__tests__/route.test.ts`.

## 2026-05-06

### T-20260506-001 - Hetzner edge bootstrap and live validation

- H.1c (managed Hetzner worker pool) and H.1d (Caddy public routing) validated
  end-to-end against real Hetzner, Tailscale, Cloudflare, and GHCR. Preview
  served from `<slug>.launchnode.de` with a Let's Encrypt cert via DNS-01.
- Added `scripts/setup-hetzner-edge.ts` to provision the Caddy edge server.
- Added `IMAGE_REGISTRY_HOST` / `IMAGE_REGISTRY_USERNAME` / `IMAGE_REGISTRY_TOKEN`
  env so worker cloud-init can `docker login` to private GHCR before pulling.
- Increased Tailscale lookup timeouts to handle Hetzner cold-boot
  (`TAILSCALE_LOOKUP_ATTEMPTS` / `TAILSCALE_LOOKUP_INTERVAL_MS`).
- Fixed `HOST_URL` double-path bug (worker heartbeats were 404), Tailscale
  description rejection, missing Caddy admin Origin header, missing srv0
  bootstrap, swallowed spawn errors, and the worker-agent Dockerfile build
  break around `node-pty`.
- Affected files: `scripts/setup-hetzner-edge.ts`,
  `lib/runtime/provisioner/cloud-init.ts`, `lib/runtime/provisioner/hetzner.ts`,
  `lib/runtime/provisioner/{hetzner,tailscale}-client.ts`,
  `lib/routing/caddy-client.ts`, `worker-agent/Dockerfile`,
  `app/api/projects/route.ts`.

## 2026-05-05

### T-20260505-015 - Managed Hetzner worker pool implemented

- Added admin-managed Hetzner workers with per-worker slot capacity, Tailscale
  provisioning, worker-agent bootstrapping, slot-based scheduling, restart
  placement, Caddy public preview routing, lifecycle hardening, and validation
  docs.
- Affected files: `prisma/schema.prisma`, `app/admin/workers/`,
  `app/api/admin/workers/`, `app/api/projects/`,
  `lib/runtime/worker-pool/`, `lib/runtime/provisioner/`,
  `lib/routing/`, `docs/AGENT_RUNTIME_OPTIONS.md`,
  `scripts/validate-hetzner-worker-pool.md`.

### T-20260504-004 - GitHub PR saveback completed

- Added sandbox Git status/push plumbing, host PR creation with fresh GitHub App
  tokens, workspace PR actions, and updated durable queue Superpowers status.
- Affected files: `app/api/projects/[id]/pull-request/`,
  `app/project/[id]/page.tsx`, `lib/github/pull-requests.ts`,
  `lib/projects/broker-git.ts`, `container/sandbox/broker/`,
  `worker-agent/`, `docs/superpowers/`.

### T-20260505-010 - Durable project agent queue implemented

- Implemented durable per-project agent run queuing with persisted run events,
  host-owned draining, worker-agent/broker streamed execution, OpenHands SDK
  conversation persistence, retry/skip blocking behavior, and queued chat UI
  replay after browser reconnect.
- Affected files: `prisma/schema.prisma`, `lib/agent-runs/`,
  `lib/runtime/worker-pool/`, `worker-agent/`,
  `container/sandbox/broker/`, `app/api/projects/[id]/runs/`,
  `app/api/projects/[id]/events/`, `app/project/[id]/page.tsx`.

### T-20260505-009 - Durable project agent queue implementation planned

- Added the implementation plan for the durable project-level agent queue,
  covering schema, APIs, worker/broker execution, OpenHands persistence,
  queued-run UI behavior, and verification.
- Affected files: `docs/superpowers/plans/2026-05-05-durable-project-agent-queue.md`,
  `docs/tasks/done/2026/05/T-20260505-009.md`.

### T-20260505-008 - Durable project agent queue designed

- Added the approved V1 design for a durable project-level FIFO agent queue,
  persisted run events, browser-independent execution, retryable failures, and
  OpenHands conversation persistence.
- Affected files: `docs/superpowers/specs/2026-05-05-durable-project-agent-queue-design.md`,
  `docs/tasks/done/2026/05/T-20260505-008.md`.

### T-20260505-007 - OpenHands runtime architecture reviewed

- Reviewed the local OpenHands SDK bridge against current SDK and Remote Agent
  Server guidance; recommended keeping the local bridge for now and treating
  Remote Agent Server as a deliberate optional runtime spike.
- Affected files: `docs/tasks/done/2026/05/T-20260505-007.md`.

### T-20260505-006 - Template sandbox provisioning diagnostics improved

- Classified known worker-agent provisioning failures into safe actionable messages and cleared the project spawn timeout after completion.
- Affected files: `app/api/projects/route.ts`, `app/api/projects/__tests__/route.test.ts`.

### T-20260505-005 - Agent Config HTTP 500 fixed locally

- Applied the Agent config migration to the local development database so the project Agent config panel can load its Prisma-backed settings.
- Affected files: `docs/tasks/done/2026/05/T-20260505-005.md`, `docs/changelog/2026-05.md`.

### T-20260505-004 - OpenHands agent configuration UI implemented

- Added global and project-level OpenHands `AGENTS.md`, skill, and file-agent management with sandbox materialization.
- Affected files: `prisma/schema.prisma`, `lib/agent-config/`, `app/api/agent-config/`, `app/api/projects/`, `components/agent-config/`, `container/sandbox/`, `docs/AGENT_RUNTIME_OPTIONS.md`.

### T-20260505-003 - OpenHands agent configuration implementation plan

- Added an implementation plan for global and project-level OpenHands
  `AGENTS.md`, skills, and file-based agent management.
- Affected files: `docs/superpowers/plans/2026-05-05-openhands-agent-configuration-ui.md`,
  `docs/tasks/done/2026/05/T-20260505-003.md`,
  `docs/changelog/2026-05.md`.

### T-20260505-002 - OpenHands skills and agents UI planned

- Researched OpenHands `AGENTS.md`, skills, and file-based agents, then planned
  global defaults plus per-project override management for the UI.
- Affected files: `docs/tasks/done/2026/05/T-20260505-002.md`,
  `docs/changelog/2026-05.md`.

### T-20260505-001 - OpenHands chat replies restored

- Fixed OpenHands bridge extraction for nested assistant message text so
  completed chat turns display replies instead of only an empty footer.
- Rebuilt the local `wbd/sandbox:dev` image so new sandboxes include the bridge
  fix.
- Affected files: `container/sandbox/broker/python/openhands_bridge.py`,
  `container/sandbox/broker/python/tests/test_openhands_bridge.py`.

## 2026-05-04

### T-20260504-020 - Preview region screenshot attachments

- Added browser-native preview region capture that crops a selected preview
  area and inserts the PNG into the existing chat image attachment draft list.
- Affected files: `app/project/[id]/page.tsx`, `lib/preview-capture.ts`,
  `lib/__tests__/preview-capture.test.ts`.

## 2026-05-04

### T-20260504-019 - Native OpenHands image attachments

- OpenHands bridge now receives chat image attachments as native multimodal
  image content via an attachment manifest, with workspace file persistence and
  vision-model validation preserved.
- Affected files: `container/sandbox/broker/src/agent-provider.ts`,
  `container/sandbox/broker/src/ws-server.ts`,
  `container/sandbox/broker/src/openhands-runner.ts`,
  `container/sandbox/broker/python/openhands_bridge.py`,
  `container/sandbox/broker/tests/openhands-runner.test.ts`,
  `container/sandbox/broker/tests/ws-server.test.ts`.

## 2026-05-04

### T-20260504-018 - OpenHands image attachment research

- Researched OpenHands native image input and identified the local bridge gap:
  images are stored as files and prompt paths today, but OpenHands needs
  `ImageContent` message blocks for direct model vision.
- Affected files: `docs/tasks/done/2026/05/T-20260504-018.md`.

## Entries Created Before Migration

## 2026-05-04

### T-20260504-016 - Split task and changelog workflow rules

- Updated `AGENTS.md` to require focused task files, monthly changelogs, and
  minimal historical file reads during normal agent work.
- Affected files: `AGENTS.md`, `docs/tasks/INDEX.md`,
  `docs/tasks/done/2026/05/T-20260504-016.md`,
  `docs/changelog/INDEX.md`, `docs/changelog/2026-05.md`.

## 2026-05-04

### T-20260504-015 - Template-Projektstart nach Projekterstellung stabilisiert

- Root Cause für `ws: closed` und verweigerte Template-Preview behoben:
  `git config` lief im Sandbox-Entrypoint vor `git init` und beendete
  Template-Container sofort.
- Entrypoint-Reihenfolge korrigiert, sodass Template-Projekte zuerst ein
  Git-Repo initialisieren und danach lokale Git-Konfiguration/Initial-Commit
  ausführen.
- Regressionstest für die Entrypoint-Reihenfolge ergänzt und lokales
  `wbd/sandbox:dev`-Image neu gebaut.

### T-20260504-014 - Workspace-Terminal auf interaktives xterm.js-PTY umgestellt

- Workspace-Terminal von Command-Formular auf xterm.js-Frontend mit
  `@xterm/xterm` und Fit-Addon umgestellt.
- Broker um interaktive `node-pty`-Sessions mit `terminal.open`,
  `terminal.input`, `terminal.resize` und `terminal.close` erweitert.
- Sandbox-Image-Build so angepasst, dass `node-pty` nativ gebaut wird; neue
  Sandboxes starten explizit eine interaktive Bash, wenn `/bin/bash` vorhanden
  ist.
- WebSocket-Test deckt PTY-Start, Eingabe, Resize, Output und Close ab.

### T-20260504-013 - Workspace-Terminal und Preview-Console ergänzt

- Broker-Protokoll um `terminal.run`, `terminal.abort`, `terminal.output` und
  `terminal.exit` ergänzt; Befehle laufen im Sandbox-Projekt-Root und werden
  während Agent-Turns gelockt.
- Workspace-Rechtsbereich um `Terminal`- und `Console`-Tabs mit Output,
  Abbruch-/Clear-Aktionen und begrenztem In-Memory-Verlauf erweitert.
- Preview-Apps erhalten automatisch eine kleine Client-Bridge, die
  `console.*`, `error` und `unhandledrejection` per `postMessage` an die
  Host-Console meldet.

### T-20260504-012 - Sandbox-Neustart per Workspace-UI ergänzt

- Projektbezogenen Restart-Endpunkt ergänzt, der die aktuelle Sandbox zerstört
  und aus derselben Template- oder GitHub-Quelle neu spawnt.
- Gespeicherte Projekt-`.env` wird beim Neustart wieder an die Sandbox
  übergeben; GitHub-Projekte erhalten dafür einen frischen Installation-Token.
- Workspace-Header um einen Restart-Button mit Ladezustand und Fehlermeldung
  ergänzt.

### T-20260504-011 - Next-Config-Erkennung für dauerhaft deaktivierte DevIndicators erweitert

- Unterstützte Next-Config-Dateinamen nach Next-Doku berücksichtigt:
  `next.config.js`, `next.config.mjs` und `next.config.ts`.
- Workspace-Devtools-Sync erkennt vorhandene Config-Dateien, erstellt bei
  fehlender Config `next.config.ts` und setzt `devIndicators` immer auf `false`.
- Debug-Button kann den Zustand nur noch reparieren/auffrischen, nicht mehr auf
  sichtbar toggeln.

### T-20260504-010 - Veraltete Top-Level-Devtools-Dateien bei `src/app`-Previews bereinigt

- Root Cause des `PageNotFoundError ... route not found /page` behoben:
  veraltetes `app/wbd-next-devtools.css` wird bei `src/app`-Projekten entfernt.
- Broker-Protokoll um `file.delete` ergänzt, inklusive Cleanup leerer
  Parent-Verzeichnisse.
- Devtools-Sync schreibt erst nach geladener File-Liste und erzeugt ohne
  bekanntes Layout keinen blinden Top-Level-`app/`-Fallback mehr.

### T-20260504-009 - Doppelte Next.js-Devtools-Anzeige in Projekt-Preview behoben

- Next.js-Dev-Indikator der Host-App deaktiviert, damit neben dem Sandbox-Iframe
  kein zweites Host-Devtools-Badge erscheint.
- Regressionstest für die Host-Next-Konfiguration ergänzt.

### T-20260504-008 - Projekt-Env-Editor in der Workspace-UI sichtbarer gemacht

- `Env`-Button zusätzlich in die Preview-Toolbar aufgenommen, damit
  projektbezogene `.env`-Variablen ohne Wechsel in den Code-Tab auffindbar sind.
- Lokale Host-Datenbank um die fehlende `ProjectEnvironment`-Tabelle ergänzt,
  damit Speichern der Projekt-Env funktioniert.

### T-20260504-007 - Next-Devtools-Dateien für `src/app`-Repos korrigiert

- Devtools-Dateipfade erkennen nun, ob ein Projekt `src/app/layout.tsx` oder
  `app/layout.tsx` verwendet.
- Workspace-Devtools schreiben die CSS-Datei in denselben App-Root wie das
  Layout, statt bei `src/app`-Repos ein störendes Top-Level-`app/` anzulegen.
- Regressionstests für beide App-Root-Varianten ergänzt.
- Die laufende Paramount-Sandbox wurde bereinigt und die Preview wieder gegen
  `src/app` gestartet.

### T-20260504-006 - GitHub-Repo-Quelle im lokalen Worker-Sandbox-Start korrigiert

- Root Cause untersucht: alte globale GitHub-Clone-Env-Werte überschrieben die
  projektspezifische Repo-Auswahl beim lokalen Worker-Sandbox-Start.
- Worker-Pool-Runtime so angepasst, dass die gewählte Projektquelle Vorrang vor
  legacy Broker-Env-Werten hat.
- Regressionstest für diesen Env-Precedence-Fall ergänzt.
- Lokales Sandbox-Image neu gebaut und das Paramount-Projekt aus dem gewählten
  GitHub-Repo neu gespawnt.

### T-20260504-005 - GitHub-Login ohne öffentliche E-Mail stabilisiert

- `email_not_found` beim GitHub-OAuth-Callback untersucht und auf fehlende
  E-Mail im GitHub-Profil zurückgeführt.
- Better-Auth-GitHub-Profilmapping ergänzt, das bei privater GitHub-E-Mail eine
  stabile interne Noreply-Adresse erzeugt.
- Regressionstest für echte und fehlende GitHub-E-Mails ergänzt.

### T-20260504-003 - GitHub-Repo-Import für lokalen Docker-Worker-Pool umgesetzt

- Better Auth mit GitHub-Login, Session-Proxy und user-scoped Projekt-Routen
  ergänzt.
- GitHub-App-Installationen, Repository- und Branch-Auswahl über Host-APIs und
  Dashboard-UI angebunden.
- Projekte können GitHub-Repos als Quelle speichern und `worker-pool-local`
  startet die Sandbox aus dem gewählten Branch mit kurzlebigem
  Installation-Token.
- Sandbox-Entrypoint klont GitHub-Repos, entfernt Token aus der Remote-URL,
  erkennt Package Manager und startet den Dev-Server im eigenen Docker-Service.
- PR-Saveback ist als `T-20260504-004` separat geplant.

### T-20260504-002 - GitHub-Auth-Env-Platzhalter ergänzt

- Better-Auth- und GitHub-App-Platzhalter in `.env.example` dokumentiert.
- Dieselben leeren Platzhalter lokal in `.env` ergänzt, ohne echte
  Secret-Werte einzutragen.

### T-20260504-001 - Turbopack-NFT-Warnung behoben

- Dynamisches Worker-Pool-Env-Dateitracing für Turbopack markiert.
- Fake-Daytona-Runtime aus dem statischen App-Route-Bundlepfad gelöst und nur
  noch lazy geladen.
- `pnpm build` läuft ohne die vorherige Turbopack-NFT-Warnung.

## 2026-05-03

### T-20260503-007 - Projekt-Env-Verwaltung umgesetzt

- Dauerhafte projektbezogene dotenv-Persistenz mit Prisma-Modell, Migration und
  gecachter-freier Projekt-Env-API ergänzt.
- Sandbox-Spawns über Worker-Pool, Daytona Cloud und Fake-Runtime um
  `.env`-Synchronisierung erweitert; Secrets werden nicht in initiale
  Sandbox-Git-Commits oder Provisioning-Fehler übernommen.
- Workspace um ein Env-Panel ergänzt, das per Textfeld speichert, die laufende
  Sandbox über den bestehenden Broker-Dateischreibpfad synchronisiert und
  Sync-Fehler retrybar hält.

### T-20260503-006 - Implementation-Plan für GitHub-Auth-Integration erstellt

- Implementation-Plan für Better Auth, GitHub App, private Repo-Imports,
  Sandbox-Boot aus Branches und PR-Erstellung ergänzt.
- Taskfolge mit Sub-Agent-Schnitten, Datei-Zuständigkeiten, Testschritten und
  Commit-Grenzen dokumentiert.
- Next.js-16-Route-Handler-/Proxy-Hinweise und die freigegebene Design-Spec als
  Planungsgrundlage referenziert.

### T-20260503-005 - Better-Auth- und GitHub-App-Integration geplant

- Design-Spec für Email/Passwort-Login, GitHub Social Login und Better Auth
  erstellt.
- GitHub App als Repo-Zugriffsschicht für persönliche Accounts und
  Organisationen festgelegt.
- Repo-Import, Sandbox-Boot aus Branch und Branch-/Commit-/PR-Flow als erster
  produktiver Milestone abgegrenzt.

### T-20260503-004 - Sub-Agent-Delegation und Reasoning-Auswahl präzisiert

- `AGENTS.md` um eine klare Sub-Agent-Delegationsregel ergänzt.
- Kriterien ergänzt, wann Arbeit lokal bleiben soll und wann sie auf
  Sub-Agents verteilt werden muss.
- Reasoning-Auswahl pro Sub-Agent mit `medium` und `high` nach Komplexität,
  Risiko und benötigtem Urteilsvermögen definiert.

### T-20260503-003 - Task-Journal und Changelog backfilled

- `TASKS.md` anhand Git-Tags, Git-Commits und `docs/superpowers` als strukturelle
  Übersicht neu aufgebaut.
- Abgeschlossene Phasen und größere Git-Blöcke von 2026-04-22 bis 2026-05-03
  mit Plan-/Spec-Verweisen und Git-Evidenz ergänzt.
- `AGENTS.md` um Regeln für kompakte Task-Tabellen und rückwirkend
  rekonstruierte Aufgaben erweitert.

### T-20260503-002 - Projektlokalen Codex Git-Commit-Agent konfiguriert

- `.codex/config.toml` und `.codex/agents/git-commit-manager.toml` ergänzt.
- Commit-Agent-Wording auf `website-builder-daytona` angepasst.

### T-20260503-001 - Projektweites Task-Journal und Changelog eingeführt

- `TASKS.md` als kanonisches Task-Journal mit stabilen Task-IDs ergänzt.
- `CHANGELOG.md` für knappe Completed-Work-Zusammenfassungen ergänzt.
- `AGENTS.md` um projektbezogene Workflow-, Verification-, Git-, Next.js- und
  Runtime-Regeln erweitert.

## 2026-05-04

### T-20260504-017 - Legacy task and changelog files migrated

- Migrated legacy `TASKS.md` entries into sharded task files and legacy `CHANGELOG.md` sections into monthly changelog files.
- Archived original legacy files at `docs/archive/TASKS.legacy.md` and `docs/archive/CHANGELOG.legacy.md`.
- Affected files: `TASKS.md`, `CHANGELOG.md`, `docs/tasks/`, `docs/changelog/`, `docs/archive/`.

## 2026-05-05

### T-20260505-011 - Durable queue branch merged and Prisma config fixed

- Fast-forward merged `feature/durable-project-agent-queue` into `main` and
  replaced Prisma config `.env` loading with the native Node API so Prisma
  Client generation works without an undeclared `dotenv` dependency.
- Affected files: `prisma.config.ts`, `docs/tasks/`, `docs/changelog/`.

### T-20260505-012 - Useful worktree changes selectively integrated

- Added authenticated OpenHands library APIs/UI, revision import/export,
  workflow preset snapshots for durable OpenHands runs, and LAN-friendly dev
  preview/proxy host configuration.
- Affected files: `app/api/library/`, `app/library/`, `components/library/`,
  `lib/library/`, `lib/agents/`, `lib/agent-runs/`, `prisma/`, `app/page.tsx`,
  `app/project/[id]/page.tsx`, `next.config.ts`, `ws-proxy/src/index.ts`.

### T-20260505-013 - Local dev database stack repaired

- Repaired local Prisma migration metadata, applied the missing durable queue
  migrations, regenerated Prisma Client, and restarted the dev stack so template
  project creation no longer returns HTTP 500.
- Affected files: `docs/tasks/`, `docs/changelog/`.

### T-20260505-014 - Managed Hetzner worker pool planned

- Added design and implementation plans for admin-managed Hetzner workers,
  slot-based project placement, elastic restart placement, and Cloudflare/Caddy
  public project routing.
- Affected files: `docs/superpowers/specs/`, `docs/superpowers/plans/`,
  `docs/tasks/`, `docs/changelog/`.

## 2026-05-07

### T-20260507-001 - Chat fetch failure diagnosed

- Identified a local `WORKER_AGENT_URL` override causing queued OpenHands runs
  to call `127.0.0.1:4500` instead of the assigned Hetzner worker.
- Affected files: `docs/tasks/`, `docs/changelog/`.

### T-20260507-002 - Stalled chat run diagnosed

- Identified the follow-up chat run as queued behind the previous blocked
  failed run; the assigned Hetzner sandbox is reachable and running.
- Affected files: `docs/tasks/`, `docs/changelog/`.

### T-20260507-003 - Queue unblock controls added

- Added project workspace controls to retry or skip a blocked durable chat run
  from the chat panel.
- Affected files: `app/project/[id]/page.tsx`, `lib/agent-runs/queue-ui.ts`,
  `lib/agent-runs/__tests__/queue-ui.test.ts`.

### T-20260507-004 - Worker broker forwarding fixed

- Made the worker-agent broker host configurable and configured local/Hetzner
  worker startup to reach host-published sandbox broker ports through
  `host.docker.internal`.
- Affected files: `worker-agent/src/`, `lib/runtime/provisioner/`,
  `scripts/start-local-worker-agent.sh`.

### T-20260507-005 - Hetzner worker provisioning recovered

- Diagnosed `worker-2` provisioning failure as an invalid worker-agent image tag
  and recovered the worker using the published `latest` image.
- Affected files: `.env`, `docs/tasks/`, `docs/changelog/`.

### T-20260507-006 - Aborted chat runs fixed

- Added a separate 30 minute worker-agent timeout for streaming agent runs so
  long OpenHands executions do not fail with `This operation was aborted` while
  the sandbox is still applying changes.
- Affected files: `lib/runtime/worker-pool/`, `lib/agent-runs/__tests__/`,
  `docs/AGENT_RUNTIME_OPTIONS.md`.

### T-20260507-007 - Fixed run timeout removed

- Removed the fixed timeout for streaming worker-agent run execution; long
  OpenHands tasks now rely on terminal events, explicit cancellation, or
  transport failure instead of elapsed wall-clock aborts.
- Affected files: `lib/runtime/worker-pool/`, `docs/AGENT_RUNTIME_OPTIONS.md`.
